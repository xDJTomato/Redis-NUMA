Redis NUMA内存池性能测试报告
================================
测试时间: 2026-02-07
Redis版本: 6.2.21
NUMA配置: 启用内存池（64KB chunk，8级大小分类）
编译选项: MALLOC=libc + HAVE_NUMA

## 1. 基础性能测试（默认50并发）
----------------------------------
SET: 166,113 req/s (p50=0.159ms)
GET: 166,113 req/s (p50=0.159ms)
INCR: 164,474 req/s (p50=0.159ms)
LPUSH: 104,932 req/s (p50=0.415ms)
RPUSH: 101,523 req/s (p50=0.423ms)
LPOP: 97,656 req/s (p50=0.431ms)
RPOP: 88,106 req/s (p50=0.487ms)
SADD: 121,065 req/s (p50=0.215ms)
HSET: 112,740 req/s (p50=0.231ms)
SPOP: 111,111 req/s (p50=0.231ms)
ZADD: 112,233 req/s (p50=0.231ms)
ZPOPMIN: 111,483 req/s (p50=0.231ms)

## 2. 并发测试
----------------------------------
单线程(c=1):
  SET: 82,645 req/s
  GET: 66,269 req/s

50并发(c=50):
  SET: 170,648 req/s
  GET: 166,113 req/s

100并发(c=100):
  SET: 163,399 req/s
  GET: 165,289 req/s

## 3. 不同数据大小测试
----------------------------------
10字节:
  SET: 174,216 req/s
  GET: 165,017 req/s

100字节:
  SET: 162,866 req/s
  GET: 160,772 req/s

1KB:
  SET: 105,708 req/s
  GET: 155,763 req/s

## 4. Pipeline测试（P=10）
----------------------------------
SET: 1,162,791 req/s (11.6x提升)
GET: 1,388,889 req/s (13.9x提升)

## 5. 性能对比分析
----------------------------------
NUMA内存池 vs 无内存池:
- SET性能: 166K vs 25K = 6.6x提升 ✓
- GET性能: 166K vs 27K = 6.1x提升 ✓
- 延迟优化: p50从1-2ms降至0.15-0.2ms

NUMA内存池 vs 标准Redis(jemalloc):
- SET性能: 166K vs 150K = 1.1x (持平或略优)
- GET性能: 166K vs 140K = 1.2x (略优)

## 6. LRANGE性能（列表范围查询）
----------------------------------
LRANGE_100: 69,061 req/s
LRANGE_300: 29,326 req/s
LRANGE_500: 20,730 req/s
LRANGE_600: 18,146 req/s

## 7. 结论
----------------------------------
✓ NUMA内存池成功实现，性能达到预期
✓ 小对象分配（≤512字节）性能提升5-6倍
✓ 与标准Redis性能相当或略优
✓ 所有数据结构操作正常
✓ Pipeline模式下可达百万级QPS

内存池设计合理：
- 64KB chunk大小适中
- 8级大小分类覆盖常见场景
- 16字节PREFIX开销可接受
- from_pool标记支持差异化释放策略

## 8. NUMAflow 公平评测（QEMU 不可用环境，本次新增）
----------------------------------

由于开发机为 16GB Windows（无 libnuma / 无异构内存 / 无 QEMU），本仓库新增了
纯 C11 的 NUMAflow 评测框架（numaflow/），在可移植 libnuma 仿真拓扑上复现同一条
访问轨迹，公平比较各调度策略。DRAM 容量约束为工作集约 50%，迫使策略做取舍。

以下数字对应仓库的单一入口 `run_full_validation.sh` 自动生成并提交的
`results/bench_*.json`/`results/report.html`（测试时间: 2026-08-20，见
docs/new/09-architecture-decisions.md ADR-10）：

20000 keys / 200000 访问 / epoch=5000 / budget=64 / 2 节点（dram0 + cxl1），
四个工作负载全量对比：

  工作负载  | 策略           | 本地命中率 | 净代价(ns) | 迁移次数
  ---------|----------------|-----------|-----------|--------
  zipf     | Baseline(noop) |   0.0%    | 504.9M    | 0
  zipf     | Composite LRU  |  65.8%    | 238.3M    | 3161
  zipf     | TinyLFU        |  65.8%    | 228.1M    | 1058
  zipf     | CAAT（默认）   |  84.5%    | 183.7M    | 13521
  hotspot  | Baseline(noop) |   0.0%    | 456.1M    | 0
  hotspot  | Composite LRU  |  56.1%    | 250.6M    | 4453
  hotspot  | TinyLFU        |  36.1%    | 326.9M    | 2586
  hotspot  | CAAT（默认）   |  76.1%    | 185.4M    | 10321
  uniform  | Baseline(noop) |   0.0%    | 467.7M    | 0
  uniform  | Composite LRU  |  19.7%    | 408.8M    | 7826
  uniform  | TinyLFU        |  13.6%    | 424.4M    | 4060
  uniform  | CAAT（默认）   |  18.0%    | 388.5M    | 4117
  temporal | Baseline(noop) |   0.0%    | 480.5M    | 0
  temporal | Composite LRU  |  23.3%    | 394.0M    | 3132
  temporal | TinyLFU        |  19.4%    | 407.4M    | 3578
  temporal | CAAT（默认）   |  48.9%    | 307.8M    | 9613

  工作负载  | CAAT vs Composite LRU | CAAT vs TinyLFU
  ---------|------------------------|-------------------
  zipf     | 净代价低 22.9%（更优） | 净代价低 19.5%（更优）
  hotspot  | 净代价低 26.0%（更优） | 净代价低 43.3%（更优）
  uniform  | 净代价低  5.0%（更优） | 净代价低  8.5%（更优）
  temporal | 净代价低 21.9%（更优） | 净代价低 24.5%（更优）

结论: 在 budget=64（相对 20000 key 的紧约束比例）下，CAAT 在全部四个工作负载
上净代价都低于 Composite LRU 和 TinyLFU，包括之前认为它会更差的 uniform 负载。

**重要补充（budget/keyspace 比例会反转这个结论，不是无条件成立）**：换一组更
宽松的参数（3000 keys / 120000 访问 / epoch=3000 / **budget=256**，即预算相对
key 数量的比例从 64/20000≈0.32% 放宽到 256/3000≈8.5%）复测，CAAT 在 uniform
负载上反而比 Composite LRU 净代价高 31.1%、在 temporal 上打平（高 3.3%），只有
在 zipf/hotspot 这类有明显冷热分层的负载上才稳定更优（细节见
docs/new/09-architecture-decisions.md ADR-09 的完整四工作负载对比表）。原因：
`demote_cold` 会主动把"冷"数据搬离 DRAM，budget 越宽松，这个动作在没有真正
冷热差异的负载（uniform）下就越容易变成纯粹浪费的迁移开销；budget 收紧后这个
副作用被自然限流，CAAT 的"晋升+降级统一净收益决策"优势就重新占上风。ADR-08 把
CAAT 设为内核默认这个决策本身不需要撤销（真实 Redis 场景的迁移预算通常远小于
键空间规模，接近本节的 budget=64 场景），但引用"CAAT 更优"时应注明参数比例，
不是无条件成立于任意 budget/keyspace 组合。

订正历史（见 ADR-09/ADR-10 的完整诊断过程）：这组数字此前经历过三个不同的错误
状态——① 最初两个 bug 都在时命中率只有 84.6%；② 只修复"频率信号从未写入"这一个
bug 后命中率到 91.1%、但 CAAT 的降级路径其实从未真正生效过（详见 ADR-09）；
③ 本节数字对应的版本同时修好了这两个 bug，并且额外发现并修复了一个会导致
NUMAflow Redis 桥接彻底挂死的严重 bug（`nf_bridge_run` 内部查找表容量硬编码
且不扩容，真实规模下会死循环，见 ADR-10）——这也是为什么本次改用
`run_full_validation.sh` 自带的、贴近真实使用场景的百万级 YCSB 端到端基准来
交叉验证，而不只依赖 numaflow 自己的合成评测。

复现:  cd numaflow && make && make bench && make report   # 生成 results/report.html
       cd numaflow && make test             # 全部单元/冒烟/桥接测试

## 9. YCSB 带宽/负载基准（本次新增，测试时间: 2026-08-19）
----------------------------------

此前的验证会话因沙箱环境没有 JDK 而跳过了这一层（见 TESTING.md 第 4 项）。本次
补装了一份便携式 JDK 17（Temurin，解压到 ~/.local/opt/jdk-17，无需 root）和便携
式 Maven 3.9.9，跑通了 tests/ycsb/run_bw_benchmark.sh 和 tests/ycsb/run_ycsb.sh。

跑的过程中遇到、并修复了两个和"沙箱只有 1 个 NUMA 节点"相关的环境问题：

1. `run_bw_benchmark.sh` 默认 `--process-nodes 0,2`（把 Redis 进程绑定到 NUMA
   节点 0 和 2），但本机 `numactl --hardware` 只有 `available: 1 nodes (0)`，
   `numactl --cpunodebind=0,2` 直接报错退出。改用脚本自带的
   `--process-nodes all`（取消节点绑定）后正常运行——这是一个真实存在、值得记住
   的坑：任何只有 1 个物理 NUMA 节点的机器上跑这个脚本，都需要这个参数。
2. 官方 YCSB 0.17.0 发行包里 `bin/ycsb`（Python 版命令行入口）是 Python 2 语法
   （`except Foo, err:`），本机只有 python3，运行会直接 SyntaxError。`bin/ycsb.sh`
   （Java 版入口，CLI 参数完全一致）不受影响——这也是 run_bw_benchmark.sh 自己的
   注释里已经提到、并主动绕开的同一个问题。把解压出来的 `bin/ycsb`（第三方发行包
   的一部分，不在本仓库版本控制中）换成一个转发到 `ycsb.sh` 的 shim 后解决。

### run_bw_benchmark.sh --process-nodes all（3 阶段）

  阶段                        | 吞吐 (ops/sec)
  ----------------------------|---------------
  Phase 1 Fill（填充）        | 9,688
  Phase 2 Hotspot Migration   | 120,602
  Phase 3 Sustained Pressure  | 130,370

结果目录: tests/ycsb/results/bw_bench_20260819_144153/
（含 metrics.csv、各阶段原始 YCSB 输出、benchmark_report.png / _latency.png）

### run_ycsb.sh --mode baseline（recordcount=100000, operationcount=100000, 4 线程）

  阶段  | 吞吐 (ops/sec) | READ 平均延迟 (us) | UPDATE 平均延迟 (us)
  ------|---------------|--------------------|--------------------
  Load  | 34,282        | —                  | —
  Run   | 73,314        | 50.0               | 52.6

结果目录: tests/ycsb/results/baseline_20260819_151756/

### run_ycsb.sh --mode stress（recordcount=1000000, operationcount=1000000, 32 线程）

  阶段  | 吞吐 (ops/sec) | READ 平均延迟 (us) | UPDATE 平均延迟 (us)
  ------|---------------|--------------------|--------------------
  Load  | 53,743        | —                  | —
  Run   | 142,005       | 212.0              | 225.3

结果目录: tests/ycsb/results/stress_20260819_151847/

结论: 三组基准全部跑通、全部为真实测得数据（非仿真/非估算）。高并发（32 线程）
      下吞吐更高、但尾延迟也相应变大，符合预期；`Phase 1 Fill` 吞吐明显低于
      Phase 2/3，是因为填充阶段是纯写入且此时迁移/热度追踪尚未建立起有效候选集。

复现:  cd tests/ycsb && ./run_bw_benchmark.sh --process-nodes all
       cd tests/ycsb && ./run_ycsb.sh --mode baseline
       cd tests/ycsb && ./run_ycsb.sh --mode stress

## 10. 迁移策略全量对比：Vanilla / Noop / Composite LRU / TinyLFU（本次新增，测试时间: 2026-08-19）
----------------------------------

新增 `tests/ycsb/run_algorithm_comparison.sh`，把 run_bw_benchmark.sh 里已有的
`--tinylfu` 开关补齐了一个对称的 `--no-migrate`（只关 Slot 1，不开 Slot 2，得到
纯 noop 基线），再串联已存在的 `run_bw_benchmark_vanilla.sh`，四组跑同一份
bw_saturate 三阶段负载（Fill→Hotspot→Sustain），maxmem 统一为 8gb（此前
vanilla 脚本默认 8gb、NUMA 脚本默认 3500mb，四组必须一致才是单一变量对比）。

顺带修了一个真实 bug：`run_bw_benchmark_vanilla.sh` 头部注释写着"Phase 参数与
CXL 版完全一致"，但 PHASE2_OPS/PHASE3_OPS 实际是 2M/6M，主脚本是 4M/4M——两边
早就不一致了，这次统一改成 4M/4M。另外 `visualize_bw_benchmark.py` 的
NAMED_PALETTE 缺 "Redis-NUMA (Noop)" 这个新标签，索引取模后刚好撞上了
"Vanilla Redis" 的同一个绿色，两条曲线在图上完全重叠分不清——补充了独立的橙色。

  组                          | Fill (ops/sec) | Hotspot (ops/sec) | Sustain (ops/sec)
  ----------------------------|----------------|--------------------|-------------------
  Vanilla Redis 7.2.6         | 22,733         | 131,419            | 131,713
  Redis-NUMA (Composite LRU)  | 41,549         | 123,977            | 126,478
  Redis-NUMA (Noop)           | 44,086         | 134,821            | 134,431
  Redis-NUMA (TinyLFU)        | 40,378         | 121,655            | 122,952

结论: 这是单节点沙箱环境（`numactl --hardware` 只有 1 个 NUMA 节点），没有真实的
跨节点带宽/延迟差异可利用，所以四组的量级基本相当，差异主要来自 NUMA 内存池分配器
本身的开销（Fill 阶段 NUMA 版比 vanilla 快 1.8-2x，是内存池 O(1) 分配 vs
jemalloc 的差异，不是迁移策略的效果）与迁移策略的运行时开销（Composite LRU /
TinyLFU 在 Hotspot/Sustain 阶段比 Noop 基线慢 6-10%，这是迁移扫描本身占用的
CPU/锁开销）。要看到迁移策略在真实多 NUMA 节点/CXL 环境下的收益（跨节点访问延迟
差异足够大时，迁移换来的本地化命中率提升才能覆盖迁移开销），需要在有 ≥2 个物理
NUMA 节点或 CXL 设备的机器上重跑本对比。

结果目录: Results/algorithms_20260819_162940/
（comparison_report.png、comparison_report_latency.png、summary.txt，以及
vanilla/ noop/ composite_lru/ tinylfu/ 四个子目录下的完整 metrics.csv 和
YCSB 原始输出）

复现:  cd tests/ycsb && ./run_algorithm_comparison.sh
